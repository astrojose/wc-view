One-line: the `aria-live="polite"` status line for feedback lifecycle changes.

```jsx
<StatusRegion tone="success" message="3 notes written to ~/.wc-view/feedback/queue.jsonl" />
```

Text always carries the meaning; tone only colours it. One per view, near the composer.
