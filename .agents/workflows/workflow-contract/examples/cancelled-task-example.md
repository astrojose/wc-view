# Cancellation Examples

- `reverted`: set status to `cancelled`, record the restored revision and environment, and verify prior behavior.
- `superseded`: set status to `cancelled` and link the replacement task or accepted design under Follow-up.

Cancellation is terminal. It does not claim successful delivery.
