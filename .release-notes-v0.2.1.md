Package: `@astrojose/wc-view`

Install/test command:

```bash
npx @astrojose/wc-view@0.2.1 --help
```

## Fixed

- Bundle Mermaid client assets for npm installs so dynamic diagram rendering works without CDN dependencies.
- Preserve CLI stream discipline by keeping feedback JSON as the default payload and sending diagnostics to stderr.

## Contributor Testing

- Use this GitHub Release and npm package version when validating contributor workflows before broader installs.
- Package artifact evidence was verified through `npm pack --dry-run` during release preflight.

## Verification

- `./node_modules/.bin/tsc --noEmit`
- `npm test`
- `npm run build`
- `npm run validate:workflow`
- `python3 .agents/skills/release-wcv/scripts/preflight_release.py --version 0.2.1`
- `npm pack --dry-run`
