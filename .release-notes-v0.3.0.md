Package: `@astrojose/wc-view`

Install/test command:

```bash
npx @astrojose/wc-view@0.3.0 --help
```

## Added

- Stream real-time feedback updates over the new `/api/events` Server-Sent Events endpoint whenever feedback is posted.

## Changed

- Update review-surface submission messaging to confirm notes were submitted to the feedback queue.

## Contributor Testing

- Use this GitHub Release and npm package version when validating contributor workflows before broader installs.
- Use the GitHub Release assets and npm pack artifact when testing before npm install.

## Verification

- `./node_modules/.bin/tsc --noEmit`
- `npm test`
- `npm run build`
- `npm run validate:workflow`
- `python3 .agents/skills/release-wcv/scripts/preflight_release.py --version 0.3.0`
- `npm pack --dry-run`
